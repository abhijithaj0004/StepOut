part of 'hide_password_cubit.dart';

class HidePasswordState {
  bool ischanged;

  HidePasswordState({required this.ischanged});
}

final class HidePasswordInitial extends HidePasswordState {
  HidePasswordInitial({required super.ischanged});
}
